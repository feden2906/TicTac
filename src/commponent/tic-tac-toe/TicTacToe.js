export default function TicTacToe (player1, player2){
console.log(player1, player2)

    return(
        <div>
            {
                // arr.map(resp=> <p>{resp}</p>)
            }
        </div>
    )
}